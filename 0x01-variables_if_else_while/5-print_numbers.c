#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int numbers;

	numbers = 5;
	while (numbers <= 9)
	{
		printf("%d\n ", numbers);
	}
	numbers++;
	return (0);
}
